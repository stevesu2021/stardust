# 星契集网站综合优化方案

结合你的网站现状——个人开发、服务器在火山云大陆节点、已完成ICP备案、曾被搜索引擎以IP形式收录，以下方案为你每个环节都挑选了唯一最适合的工具和操作路径。


## 一、SEO修复：解决IP收录问题

搜索引擎以IP形式收录星契集，核心原因是爬虫早期通过IP地址抓取过网站，且当时未设置重定向引导。处理的核心手段是**通过301永久重定向，让搜索引擎明确域名是唯一正式版本**。

### 操作步骤

**① Nginx 配置 301 重定向**

在Nginx配置中，为IP访问添加一个独立的 `server` 块：

```nginx
server {
    listen 80;
    listen [::]:80;
    server_name 你的服务器IP;  # 替换为实际IP
    return 301 http://xingqiji.xyz$request_uri;
}
```

如果已启用HTTPS，同样需要为IP的443端口添加重定向配置。这样，任何通过IP访问的请求都会被永久重定向到域名。

**② 添加 canonical 标签**

在网站所有页面的`<head>`中添加：

```html
<link rel="canonical" href="http://xingqiji.xyz/页面路径" />
```

这向搜索引擎明确声明：域名版本才是该页面的首选URL。

**③ 百度搜索资源平台提交**

- 添加并验证你的域名 `xingqiji.xyz`
- 使用“普通收录”提交域名首页和重要页面
- 在IP资源下使用“移除”工具，临时屏蔽IP地址的索引

百度统计与百度搜索、百度百科等产品深度打通，对国内站点的SEO监控和搜索词分析有不可替代的价值。

**④ 检查内部链接与 robots.txt**

确保网站源代码中所有内部链接、图片引用、CSS/JS引用都使用相对路径或域名路径，绝对不要出现IP地址。同时在IP对应的网站根目录放置 `robots.txt`，禁止搜索引擎抓取IP站点：

```
User-Agent: *
Disallow: /
```

完成上述配置后，搜索引擎通常需要**几天到两周**的时间重新抓取和更新索引。


## 二、数据统计：宝塔网站统计（bt-stats）

你已经在使用宝塔面板管理服务器，因此**宝塔网站统计（bt-stats）** 是最贴合你技术栈的选择。它完全免费，数据存储在自有服务器，不经过第三方，从根本上解决了数据隐私和合规隐患。

### 安装与配置

在宝塔面板「软件商店」中搜索“宝塔网站统计”并安装，然后在插件设置中添加站点，自动注入跟踪脚本，无需手动修改代码。

### 核心功能

宝塔网站统计覆盖了个人网站所需的全部核心指标：

- **概览看板**：PV、UV、IP、跳出率、平均访问时长、活跃用户趋势
- **来源分析**：来源域名 + 渠道分布（直接/搜索引擎/社交/外链/付费/UTM）
- **访客分析**：地域、浏览器、OS、设备等6维分布
- **实时访客**：实时访问列表 + 访问轨迹追踪
- **页面性能**：Web Vitals（LCP/INP/CLS）均值与趋势
- **热力图**：页面点击坐标热度分布

### 技术优势

它采用Go采集后端 + DuckDB列存引擎，无需额外安装MySQL/PostgreSQL，嵌入式数据库即开即用，单机可承载日均百万PV。插件会自动在站点的Nginx配置中注入跟踪脚本，部署后访问站点任意页面，打开浏览器开发者工具→网络，查看是否存在 `bt-stats.gif` 请求，状态码200即表示成功。


## 三、安全防护：腾讯云 EdgeOne 免费版

你的域名已完成ICP备案，服务器在火山云大陆节点。综合考虑**防护能力、国内访问速度、零成本**，**腾讯云 EdgeOne 免费版**是最适合你的方案。

### 为什么选 EdgeOne 而不是其他

Cloudflare免费版不提供中国大陆节点，国内流量会被调度到境外，延迟飙升到5-8秒，且可能触发备案拦截，不适合你的场景。EdgeOne免费版是**全球首个包含中国大陆访问的免费CDN**，依托3200+全球节点（其中中国大陆2300+），与中国电信、联通、移动直连对等。

实测国内访问速度提升显著：北京从6.8秒压缩到1.2秒，上海从6.2秒压缩到1.0秒。

### 免费版包含的能力

- **CDN加速**：不限量安全加速流量
- **DDoS防护**：平台级基础L3/L4防护
- **WAF**：基础Web应用防火墙规则，拦截SQL注入、XSS等常见攻击
- **SSL证书**：免费自动管理
- **Bot管理**：基础Bot防护
- **实时分析**：7天数据保留

### 接入方式

在EdgeOne控制台添加域名 `xingqiji.xyz` → 选择免费版套餐 → 配置源站（填火山云服务器的IP） → 将域名DNS解析的A记录改为CNAME，指向EdgeOne提供的地址。域名已完成ICP备案，可直接使用中国大陆节点，无需升级付费套餐。

### 补充：火山云 DDoS 基础防护（零成本加固）

在部署EdgeOne的同时，建议在火山云控制台确认DDoS基础防护已开启。火山云为云上公网资源**免费提供最高2Gbps的DDoS防护能力**，实时监控攻击情况，秒级响应。这是零成本的底层保障，无需额外操作即可自动生效。


## 四、最终方案一览

| 事项 | 唯一推荐方案 | 核心优势 |
| :--- | :--- | :--- |
| **SEO修复** | Nginx 301重定向 + canonical标签 + 百度搜索资源平台提交 | 从根本上让搜索引擎统一收录域名 |
| **数据统计** | 宝塔网站统计（bt-stats） | 自托管、数据自主、免费、功能全面、与宝塔无缝集成 |
| **安全防护** | 腾讯云 EdgeOne 免费版 + 火山云DDoS基础防护 | 大陆节点CDN加速 + DDoS/WAF防护 + 备案域名直接可用 |

### 实施顺序

**第一步（约30分钟）**：配置Nginx 301重定向，解决IP收录问题；在百度搜索资源平台提交域名和Sitemap；确认火山云DDoS基础防护已开启。

**第二步（约1小时）**：在宝塔面板安装“宝塔网站统计”插件，添加站点并验证数据上报；注册腾讯云EdgeOne，添加域名，配置源站和CNAME解析。

**第三步（持续）**：定期查看统计数据和搜索引擎控制台，根据数据调整SEO策略；关注EdgeOne控制台的WAF拦截记录。
